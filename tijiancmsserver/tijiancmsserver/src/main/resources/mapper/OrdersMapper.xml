<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neusoft.tijaincms.mapper.OrdersMapper">
    <resultMap type="Orders" id="ordersResultMap">
        <id property="orderId" column="orderId"/>
        <result property="orderDate" column="orderDate"/>
        <result property="userId" column="userId"/>
        <result property="hpId" column="hpId"/>
        <result property="smId" column="smId"/>
        <result property="state" column="state"/>
        <association property="setmeal" column="smId" javaType="Setmeal"
                     select="com.neusoft.tijaincms.mapper.SetmealMapper.getSetmealByIdByMapper"/>

        <association property="users" column="userId" javaType="Users"
                     select="com.neusoft.tijaincms.mapper.UsersMapper.getUsersById"/>

        <association property="hospital" column="hpId" javaType="Hospital"
                     select="com.neusoft.tijaincms.mapper.HospitalMapper.getHospitalById"/>
    </resultMap>
    <select id="getOrdersCount" parameterType="OrdersPageRequestDto" resultType="int">
        select count(*)
        from orders o left join users u
        on o.userId = u.userId
        <where>
            <if test="userId != null and userId != ''">
                and u.userId = #{userId}
            </if>
            <if test="realName != null and realName != ''">
                and u.realName like concat('%',#{realName},'%')
            </if>
            <if test="sex !=null">
                and u.sex=#{sex}
            </if>
            <if test="smId != null ">
                and o.smId = #{smId}
            </if>
            <if test="orderDate != null and orderDate!='' ">
                and o.orderDate = #{orderDate}
            </if>
            <if test="state != null ">
                and o.state = #{state}
            </if>
        </where>
    </select>

    <select id="listOrders" parameterType="OrdersPageRequestDto" resultMap="ordersResultMap">
        select o.*
        from orders o left join users u
        on o.userId = u.userId
        <where>
            <if test="userId != null and userId != ''">
                and u.userId = #{userId}
            </if>
            <if test="realName != null and realName != ''">
                and u.realName like concat('%',#{realName},'%')
            </if>
            <if test="sex !=null">
                and u.sex=#{sex}
            </if>
            <if test="smId != null ">
                and o.smId = #{smId}
            </if>
            <if test="orderDate != null and orderDate!='' ">
                and o.orderDate = #{orderDate}
            </if>
            <if test="state != null ">
                and o.state = #{state}
            </if>
        </where>
        order by o.orderId
        limit #{beginNum},#{maxPageNum}
    </select>
    
    <select id="getOrdersById" parameterType="int" resultMap="ordersResultMap">
        select *
        from orders
        where orderId = #{orderId}
    </select>
</mapper>