<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neusoft.tijaincms.mapper.SetmealDetailedMapper">
    <resultMap type="SetmealDetailed" id="setmealDetailedResultMap">
        <id property="sdId" column="sdId"/>
        <result property="smId" column="smId"/>
        <result property="ciId" column="ciId"/>
        <association property="checkItem"  javaType="CheckItem"
                     select="com.neusoft.tijaincms.mapper.CheckItemMapper.getCheckItemById" column="ciId"/>
    </resultMap>
    <select id="listSetmealDetailedBySmId" parameterType="int" resultMap="setmealDetailedResultMap">
        select * from setmealDetailed where smId=#{smId} order by sdId
    </select>
</mapper>
