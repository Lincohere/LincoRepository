<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neusoft.tijaincms.mapper.SetmealMapper">
    <resultMap type="Setmeal" id="setmealResultMap">
        <id property="smId" column="smId"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="price" column="price"/>
        <collection property="sdList" ofType="SetmealDetailed"
                    select="com.neusoft.tijaincms.mapper.SetmealDetailedMapper.listSetmealDetailedBySmId" column="smId"></collection>
    </resultMap>

    <select id="getSetmealById" parameterType="int" resultMap="setmealResultMap">
        select * from setmeal where smId=#{smId}
    </select>
</mapper>
